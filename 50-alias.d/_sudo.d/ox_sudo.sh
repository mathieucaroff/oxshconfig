# Alias: sudo-like
alias so='sudo '
alias se='sudo !!'
# a s='sudo ' # sudo vs ls
# a so='sudo '"$P"'/o ' # sudo graphical
# a os='so'
# a kds='kdesudo '
# a gks='gksudo '
# a oks='o kds ' # Exec kdesudo, removing output
# a ogs='o gks ' # ~ for gksudo

# Alias: su-like
alias S='sudo su'
alias SS='sudo -Es'
# a sconda='s /opt/miniconda3/bin/conda'
alias sb='sudo bash ' # Like su or sudo -s
alias sz='sudo zsh ' # Like su or sudo -s
alias sf='sudo fish '